# Copy to frontend/.env if you need to override a default.
# The app works with zero configuration against a locally running backend.

VITE_API_BASE_URL=http://localhost:8000/api
VITE_MAP_TILE_URL=https://tile.openstreetmap.org/{z}/{x}/{y}.png
